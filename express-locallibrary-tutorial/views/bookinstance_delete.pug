extends layout 

block content 
    h1 #{title}: #{book_copy._id}

    p Do you really want to delete this book copy?


        form(method='POST' action='')
            div.form-group
                input#copyid.form-control(type='hidden',name='copyid', required='true', value=book_copy._id )

            button.btn.btn-primary(type='submit') Delete